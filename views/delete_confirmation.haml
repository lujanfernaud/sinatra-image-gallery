.header-margin
  .text-center
    %h1 Delete Image?

%img{src: @image.file, width: "100%"}

.text-center
  %h2
    %a{href: "/images/#{@image.id}/delete"}
      %button(class="btn btn-primary") Yes, Delete